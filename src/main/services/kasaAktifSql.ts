/** Aktif (soft-silinmemiş) kasa satırları — listeler ve mali toplamlar. */
export const KASA_AKTIF_SQL = `AND silinme_tarihi IS NULL`;
export const OFIS_KASA_AKTIF_SQL = `AND silinme_tarihi IS NULL`;
